#include<stdio.h>
#include<stdlib.h>

int main(void)
{
	int ligne = 0, colonne = 0;
	int hauteur = 0, base = 0;
    // int cote = 0;
    int x = 0;
	// printf("Entrer la longueur du carre\n");
	// scanf("%d",&cote);
	// if(cote < 0)
	// {
	//     cote = cote*-1; 
	// }	
	// x = cote - 1;
	// for(ligne = 0; ligne < cote; ligne++)
	// {
	// 	for(colonne = 0; colonne < cote; colonne++)
	// 	{
	// 		if(ligne == 0 || ligne == x || colonne == 0 || colonne == x)
	// 		    printf("* ");
	// 		else
	// 			printf("  ");
	// 	}
	// 	printf("\n");
	// }

	// printf("Entrer la hauteur du triangle\n");
	// scanf("%d",&hauteur);
	// printf("Entrer la base du triangle\n");
	// scanf("%d",&base);
	// if(hauteur < 0)
	// 	hauteur = hauteur * -1;
	// if(base < 0)
	// 	base = base * -1;
	// for(ligne = 1; ligne < hauteur; ligne++)
	// {
	// 	x = ligne;
	// 	for(colonne = 1; colonne <= ligne; colonne++)
	// 	{
	// 	    if(ligne == 0 || ligne == (hauteur-1) || colonne == 1 || colonne == ligne)
	// 		    printf("* ");
	// 		else
	// 			printf("  ");
	// 	}
	// 	printf("\n");
	// }

	return 0;
}